my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
  pokeApi {
    timeout = 30s
  }
}

twitter {
  key = ""
  secret = ""
  token = ""
  tokenSecret = ""
  bearer = ""
}